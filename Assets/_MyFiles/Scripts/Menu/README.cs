using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This file is to be edited or deleted.
/// Ideas for the menu script.
/// 
/// Have the menu script reference the player stats so if the user tries to include a new UI, they can access the attributes values they need to display them in their UI
/// Have the menu script reference the unassigned stats/skills/spells so the menu can edit them.
/// </summary>


public class README : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
