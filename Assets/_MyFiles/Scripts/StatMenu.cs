using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using CharacterSpace;

public class StatMenu : MonoBehaviour
{
    Canvas statMenu;
    PlayerStats player;

    public bool strButton;
    public bool vitButton;
    public bool agiButton;
    public bool dexButton;
    public bool intButton;
    public bool wisButton;
    public bool chaButton;
    public bool chaosButton;

    private void OnMouseUp()
    {
        if(strButton)
        {
            
        }
        if (vitButton)
        {

        }
        if (agiButton)
        {

        }
        if (dexButton)
        {

        }
        if (intButton)
        {

        }
        if (wisButton)
        {

        }
        if (chaButton)
        {

        }
        if (chaosButton)
        {

        }
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
